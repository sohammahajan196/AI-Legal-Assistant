# Core API
fastapi==0.139.0
uvicorn[standard]==0.51.0

# Config & validation
pydantic==2.13.4
pydantic-settings==2.14.2

# RAG orchestration
langchain
langchain-community
langchain-google-genai
langchain-huggingface

# Retrieval
faiss-cpu
rank-bm25
sentence-transformers

# Storage / caching / rate limiting
sqlalchemy
redis
slowapi

# Utilities
python-dotenv==1.2.2
httpx==0.28.1

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
fakeredis

# NOTE (T02): fastapi/uvicorn/pydantic/pydantic-settings/httpx/pytest/pytest-asyncio
# are pinned to the versions verified to boot the app + run tests in a fresh
# virtualenv (see README). Remaining deps (langchain*, faiss-cpu, sentence-transformers,
# sqlalchemy, redis, slowapi, fakeredis) are introduced by later tasks and will be
# pinned via `pip freeze` when those tasks first install/verify them.
