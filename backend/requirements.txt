# Core API
fastapi==0.139.0
uvicorn[standard]==0.51.0

# Config & validation
pydantic==2.13.4
pydantic-settings==2.14.2

# RAG orchestration
langchain-classic==1.0.8
langchain-community==0.4.2
langchain-google-genai==4.2.7
langchain-huggingface==1.2.2

# Retrieval
# CPU-only PyTorch (must precede sentence-transformers so ST resolves against this wheel).
--extra-index-url https://download.pytorch.org/whl/cpu
faiss-cpu==1.14.3
rank-bm25==0.2.2
torch==2.13.0+cpu
sentence-transformers==5.6.0

# Storage / caching / rate limiting
sqlalchemy==2.0.51
redis==8.0.1
slowapi==0.1.10

# Utilities
python-dotenv==1.2.2
httpx==0.28.1
pdfplumber==0.11.10

# All runtime deps above are pinned to versions verified in the project
# virtualenv (boot + pytest). Bump intentionally and re-run the suite / smoke
# chat before merging. Test-only deps (pytest, fakeredis, ...) live in
# requirements-dev.txt so they don't ship in the production image.
