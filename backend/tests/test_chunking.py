"""Tests for app.rag.chunking. See TASKS.md T11."""

# TODO: add unit tests once app.rag.chunking.parse_act_text is implemented,
# e.g. parsing a sample IPC excerpt into the correct section chunks with
# correct section_number values, and sub-clause splitting for long sections.
