/**
 * Next.js configuration.
 * See PLAN.md Section 12 and TASKS.md T41.
 */

/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "standalone",
};

module.exports = nextConfig;
